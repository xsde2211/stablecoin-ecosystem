# Base URL of the Node BFF in server/ (NOT the gateway directly — the BFF
# is what exposes the dynamic per-network routes and forwards everything
# else to your gateway). Run `npm start` inside server/ and point this at
# wherever it's listening.
VITE_API_BASE_URL=http://localhost:4000
