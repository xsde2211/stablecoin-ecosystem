# URL of the gateway service (services/gateway) — the admin app talks only to
# the gateway, never directly to admin-service, matching how the mobile app
# talks to it too.
VITE_API_URL=http://localhost:3001
